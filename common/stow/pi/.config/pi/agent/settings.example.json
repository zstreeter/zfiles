{
  "_comment": [
    "Template for ~/.config/pi/agent/settings.json, which is NOT tracked: pi",
    "rewrites it on every install and model switch, and it carries whichever",
    "provider and model this machine happens to use. Copy it, then run",
    "`pi install` for each entry below, or just let pi install them on start.",
    "Providers and credentials belong in models.json, which is also untracked."
  ],
  "packages": [
    "npm:pi-web-access",
    "npm:pi-lens",
    "npm:@companion-ai/feynman",
    "npm:@narumitw/pi-herdr",
    "npm:@juicesharp/rpiv-ask-user-question",
    "npm:opencode-pi",
    "git:https://github.com/hasit/pi-community-themes"
  ],
  "theme": "catppuccin-macchiato"
}
