# =============================================================================
# Gmail (Personal)
# =============================================================================
[accounts.gmail]
default = true
folder = "INBOX"

on-message-added.cmd = "notify-send 'Personal Email' 'New mail from {sender}' -c 'email.personal' -i 'mail-message-new'"
on-message-added.notify.summary = "📫 {sender}"
on-message-added.notify.body = "{subject}"

backend.type = "imap"
backend.host = "imap.gmail.com"
backend.port = 993
backend.encryption.type = "tls"
backend.login = "zacharylouis42@gmail.com"
backend.auth.type = "password"
backend.auth.cmd = "pass show email/gmail-himalaya"

# =============================================================================
# Work (JBI Tools)
# =============================================================================
[accounts.work]
folder = "INBOX"

on-message-added.cmd = "notify-send 'Work Email' 'New mail from {sender}' -c 'email.work' -i 'mail-message-new' -u critical"
on-message-added.notify.summary = "📫 {sender}"
on-message-added.notify.body = "{subject}"

backend.type = "imap"
backend.host = "imap.hostinger.com"
backend.port = 993
backend.encryption.type = "tls"
backend.login = "zach@jbi.tools"
backend.auth.type = "password"
backend.auth.cmd = "pass show email/work-himalaya"
