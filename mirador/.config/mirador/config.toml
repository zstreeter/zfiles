# =============================================================================
# Gmail (Personal)
# =============================================================================
[accounts.gmail]
default = true
folder = "INBOX"

backend.type = "imap"
backend.host = "imap.gmail.com"
backend.port = 993
backend.login = "zacharylouis42@gmail.com"
# Changed type to 'command' to support 'pass'
backend.auth.type = "command"
backend.auth.cmd = "pass show email/gmail-himalaya"

[accounts.gmail.on-message-added]
# Using 'sh -c' is sometimes safer for complex args, but direct cmd usually works
cmd = "notify-send 'Personal Email' 'You have new mail.' -i mail-message-new"

# =============================================================================
# Work (JBI Tools)
# =============================================================================
[accounts.work]
folder = "INBOX"

backend.type = "imap"
backend.host = "imap.hostinger.com"
backend.port = 993
backend.login = "zach@jbi.tools"
# Changed type to 'command'
backend.auth.type = "command"
backend.auth.cmd = "pass show email/work-himalaya"

[accounts.work.on-message-added]
# Sticky (-t 0) is great for work
cmd = "notify-send 'Work Email' 'You have new mail.' -i mail-message-new -t 0"
